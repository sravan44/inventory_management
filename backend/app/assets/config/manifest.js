// Sprockets manifest — unused while RailsAdmin (the only asset consumer) is
// deferred (ADR-0014). Kept as a placeholder; when RailsAdmin returns and brings
// Sprockets back, link its assets here.
